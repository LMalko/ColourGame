<!DOCTYPE html>
<html>
<head>
    <title>Campgrounds</title>
    <link rel="stylesheet" href="/scripts/bootstrap.css">
    <link rel="stylesheet" href="/stylesheets/main.css">

</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark navbarcolors bodyNavbarMargin fixed-top">

    <a class="navbar-brand whiteTextColor ml-2" href="/">Home</a>


        <!-- parameter 'data-target ' sets id which items will be inside hamburger -->
        <button class="navbar-toggler" type="button"
                data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">


                <a class="whiteTextColor <%= thisURL === "/campgrounds/" ? "redTextColor": " "%> ml-2"
                                href="/campgrounds">Campgrounds</a>

            <ul class="navbar-nav mr-auto">
                <% if(currentUser) {%>
                <li class="nav-item dropdown ml-2">
                    <a class="nav-link dropdown-toggle"
                       href="#" id="dropdownSpecs1" role="button"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Available actions
                    </a>
                    <div class="dropdown-menu" id="dropdownSpecs4" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" id="dropdownSpecs2"
                           href="/users/<%= currentUser._id%>">Show my profile</a>
                        <a class="dropdown-item" id="dropdownSpecs2"
                           href="/users/<%= currentUser._id%>/editAvatar">Change avatar</a>
                <% if(currentUser.isAdmin){ %>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" id="dropdownSpecs2" href="/editUser">Assign user roles</a>
                        <a class="dropdown-item" id="dropdownSpecs3" href="/destroyUser">Delete user</a>
                    </div>
                </li>
                <%} %>
            <%}%>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <%if(!currentUser){%>
                <li><a class="whiteTextColor <%= thisURL === "/register" ? "redTextColor": " "%> ml-2 mr-2"
                       href="/register">Sign Up</a></li>
                <li><a class="whiteTextColor <%= thisURL === "/login" ? "redTextColor": " "%> ml-2 mr-2"
                       href="/login">Log In</a></li>
                <%}else{%>
                <li class="limeTextColor ml-2 mr-3" >
                    <a>CURRENT USER: <%= currentUser.username.toUpperCase() %></a>
                </li>
                <li class="ml-2 mr-3"><a class="whiteTextColor"href="/logout">Log Out</a></li>
                <%}%>
            </ul>
        </div>
</nav>



<div class="container dummyContainer">

</div>

<!--Flash message-->
<div class="container">
    <!--Although error is empty, it's not undefined-->
    <% if(error && error.length > 0){%>
    <div class="alert alert-danger" role="alert">
        <%= error %>
    </div>
    <%}%>

    <% if(success && success.length > 0){%>
    <div class="alert alert-success" role="alert">
        <%= success %>
    </div>
    <%}%>

    <% try{ %>
        <% if (results === 'No results.') { %>
                <div class="alert alert-danger" role="alert">
                    <%= results %>
                </div>
        <% } else if(results) { %>
                <div class="alert alert-success" role="alert">
                    <%= results %>
                </div>

        <% }}catch(err){console.log(err)}%>
</div>

