<% include ../partials/header %>

<div class="container" style="text-align: center;">
    <h1>Edit comment<%= campground.name %></h1>

    <div class="row" >
        <div class="NewPost">
            <!-- REST way to code 'PUT'-->
            <form
                    <!-- campground_id will be passed by router.get when rendering.-->
                    action="/campgrounds/<%= campground_id %>/comments/<%= comment._id%>?_method=PUT"
                    method="POST">
                <div class="form-group">
                    <!--Change placeholder to value to display current attribute-->
                    <!--Group attributes together in campground[] to pas to UPDATE-->
                    <input class="form-control" type="text" name="comment[text]" value="<%= comment.text%>">
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-info btn-block">Submit</button>
                </div>
            </form>
        </div>
    </div>
    <a href="/campgrounds">Go back</a>
</div>


<% include ../partials/footer %>